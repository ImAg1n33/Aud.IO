# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Secrets
.env
.env.*
!.env.example

# Runtime data (never commit personal data — episodes, profiles, chroma, logs, cache)
backend/data/
backend/.chroma_cache/
backend/qrcode.png

# Frontend
node_modules/
dist/
coverage/

# OS and editors
.DS_Store
Thumbs.db

# Architecture diagnostic reports (local snapshots, periodically regenerated)
docs/architecture-reports/

# Eval reports (regenerated on every eval run)
eval/reports/

# IDE (personal workspace settings, not portable)
.vscode/

# Claude Code / Codex (local workspace settings + agent skills)
.claude/
.agents/
